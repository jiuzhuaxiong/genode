This repository contains the implementation of Genode for the SPARTAN
kernel.

After using 'make prepare' please refer to the create_builddir script in 
order to build the system.
Currently included tests running automatically with the 'make run/*' command 
are the 'hello' and 'printf' test.
